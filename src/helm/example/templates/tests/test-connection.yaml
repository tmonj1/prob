apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "probchart.fullname" . }}-test-connection"
  labels:
    {{- include "probchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "probchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
