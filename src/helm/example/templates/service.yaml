apiVersion: v1
kind: Service
metadata:
  name: {{ include "probchart.fullname" . }}
  labels:
    {{- include "probchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "probchart.selectorLabels" . | nindent 4 }}
